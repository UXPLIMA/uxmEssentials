/**
 * The survival context's bukkit-adapter wiring: {@link com.uxplima.uxmessentials.survival.adapter.SurvivalWiring}
 * constructs the harvesting mechanics over the kernel ports and produces the {@code /treefeller} and
 * {@code /veinminer} toggle commands and the {@code BlockBreakEvent} listeners the plugin registers. This is the one
 * place the survival context is wired: nothing else news up its classes.
 */
@org.jspecify.annotations.NullMarked
package com.uxplima.uxmessentials.survival.adapter;
