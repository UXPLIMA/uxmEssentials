/**
 * The villagers context's bukkit-adapter wiring:
 * {@link com.uxplima.uxmessentials.villagers.adapter.VillagersWiring} constructs the trade-availability features over
 * the kernel ports and produces the trade / interact listeners and the restock sweep the plugin registers, each gated
 * by its config switch. This is the one place the villagers context is wired: nothing else news up its classes.
 */
@org.jspecify.annotations.NullMarked
package com.uxplima.uxmessentials.villagers.adapter;
